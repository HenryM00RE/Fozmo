# DSD Test Snapshot

The checked-in test run completed its full set of digital checks without
converter errors.

It covered normal and tiny signals, silence, demanding passages, restarts and
high-resolution input. This is a software result only; it says nothing about
the analog output of a particular DAC or which setting anyone should prefer.

To produce current results, run the command in
[PCM-to-DSD Verification](../dsd-public-quality.md).
